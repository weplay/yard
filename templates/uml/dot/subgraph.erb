subgraph cluster_<%= format_path object %> {
  label = "<%= h(object.name) unless object.path == "" %>"; labelloc=b;
  <% for obj in namespaces(object) %>
    <%= render(_subgraph(obj), :object => obj, &block) %>
  <% end %>
}